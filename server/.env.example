# development | test | production. Auth rate limiting is active in production only.
NODE_ENV="development"
PORT=3000
DATABASE_URL="postgresql://underscore:underscore@localhost:5432/underscore?schema=public"
# Required, min 32 chars, no default — generate with: openssl rand -base64 32
BETTER_AUTH_SECRET=
BETTER_AUTH_URL="http://localhost:3000"
APP_SCHEME="underscore"
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
SPOTIFY_CLIENT_ID=
SPOTIFY_CLIENT_SECRET=
ANTHROPIC_API_KEY=
GOOGLE_BOOKS_API_KEY=
# Upstream roots. Unset means the real endpoint; override to point at a fixture server.
# GOOGLE_BOOKS_BASE_URL="https://www.googleapis.com/books/v1"
# ANTHROPIC_BASE_URL=
# SPOTIFY_ACCOUNTS_BASE_URL="https://accounts.spotify.com"
# SPOTIFY_API_BASE_URL="https://api.spotify.com/v1"
APP_ORIGIN="http://localhost:8081"
# Local development only — the seed refuses to run when NODE_ENV=production
SEED_USER_EMAIL=
SEED_USER_PASSWORD=
